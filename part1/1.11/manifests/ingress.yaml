apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pong-log-app-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: pong-log-app-svc
            port:
              number: 2345
      - path: /pingpong
        pathType: Prefix
        backend:
          service:
            name: pong-log-app-svc
            port:
              number: 8020